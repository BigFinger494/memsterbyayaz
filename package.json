{
  "name": "MemsterByMe",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd client && npm start && cd .. && node ./serverSide/bin/www"
  },
  "dependencies": {
    
  },
  "devDependencies": {
    
  }
}
